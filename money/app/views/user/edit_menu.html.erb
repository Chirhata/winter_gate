<div class="edit_menu top-main">
  <div class="edit_menu message">
    <h1>ユーザー設定変更</h1>
    <% @user_name = User.find_by(id: session[:user_id])%>
    <%= link_to("名前変更", "/user/#{@user_name.id}/edit_menu/name") %>
    <%= link_to("パスワード変更", "/user/#{@user_name.id}/edit_menu/password") %>
  </div>
</div>
